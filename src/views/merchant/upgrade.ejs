<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Upgrade to Merchant</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <%- include('../partials/topnav', { title: 'Upgrade to Merchant', subtitle: 'Create your merchant profile', containerClass: 'max-w-3xl' }) %>

    <div class="max-w-3xl mx-auto px-4 py-6 space-y-4">
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 text-red-800 text-sm"><%= error %></div>
      <% } %>

      <div class="bg-white border rounded-xl shadow-sm p-5">
        <div class="text-sm font-semibold text-gray-900">Merchant benefits</div>
        <div class="mt-1 text-sm text-gray-600">
          Merchants can manage their store and publish group-buy packages/services, while keeping all user features.
        </div>
      </div>

      <form class="bg-white border rounded-xl shadow-sm p-5 grid grid-cols-1 md:grid-cols-2 gap-4" method="post" action="/merchant/onboard">
        <div class="md:col-span-2">
          <label class="block text-sm font-medium text-gray-700 mb-1">Store name *</label>
          <input name="storeName" required value="<%= form?.storeName || '' %>" class="w-full px-3 py-2 border rounded-lg" />
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Contact name</label>
          <input name="contactName" value="<%= form?.contactName || '' %>" class="w-full px-3 py-2 border rounded-lg" />
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">WeChat</label>
          <input name="wechat" value="<%= form?.wechat || '' %>" class="w-full px-3 py-2 border rounded-lg" />
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Opening hours</label>
          <input name="openHours" value="<%= form?.openHours || '' %>" placeholder="e.g. Mon-Fri 09:00-18:00" class="w-full px-3 py-2 border rounded-lg" />
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Address</label>
          <input name="address" value="<%= form?.address || '' %>" class="w-full px-3 py-2 border rounded-lg" />
        </div>
        <div class="md:col-span-2">
          <label class="block text-sm font-medium text-gray-700 mb-1">Store description</label>
          <textarea name="description" rows="4" class="w-full px-3 py-2 border rounded-lg"><%= form?.description || '' %></textarea>
        </div>

        <div class="md:col-span-2 flex items-center gap-3">
          <button class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700" type="submit">Upgrade</button>
          <a href="/account" class="px-4 py-2 rounded-lg border text-gray-700 hover:bg-gray-50">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>

