<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Merchant Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <%- include('../partials/topnav', { title: 'Merchant Dashboard', subtitle: merchant.name, containerClass: 'max-w-6xl' }) %>

    <div class="max-w-6xl mx-auto px-4 py-6 space-y-6">
      <div class="bg-white border rounded-xl shadow-sm p-5">
        <div class="flex items-center justify-between">
          <div class="text-sm font-semibold text-gray-900">Store profile</div>
          <form method="post" action="/logout">
            <button class="px-4 py-2 rounded-lg border text-gray-700 hover:bg-gray-50" type="submit">Logout</button>
          </form>
        </div>

        <form class="mt-4 grid grid-cols-1 md:grid-cols-2 gap-4" method="post" action="/merchant/profile" enctype="multipart/form-data">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Store name</label>
            <input name="name" value="<%= merchant.name %>" class="w-full px-3 py-2 border rounded-lg" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Contact name</label>
            <input name="contactName" value="<%= merchant.contactName || '' %>" class="w-full px-3 py-2 border rounded-lg" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Phone</label>
            <input name="phone" value="<%= merchant.phone || '' %>" class="w-full px-3 py-2 border rounded-lg" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
            <input name="email" value="<%= merchant.email || '' %>" class="w-full px-3 py-2 border rounded-lg" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">WeChat</label>
            <input name="wechat" value="<%= merchant.wechat || '' %>" class="w-full px-3 py-2 border rounded-lg" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Opening hours</label>
            <input name="openHours" value="<%= merchant.openHours || '' %>" placeholder="e.g. Mon-Fri 09:00-18:00" class="w-full px-3 py-2 border rounded-lg" />
          </div>
          <div class="md:col-span-2">
            <label class="block text-sm font-medium text-gray-700 mb-1">Address</label>
            <input name="address" value="<%= merchant.address || '' %>" class="w-full px-3 py-2 border rounded-lg" />
          </div>
          <div class="md:col-span-2">
            <label class="block text-sm font-medium text-gray-700 mb-1">Store description</label>
            <textarea name="description" rows="4" class="w-full px-3 py-2 border rounded-lg"><%= merchant.description || '' %></textarea>
          </div>
          <div class="md:col-span-2">
            <label class="block text-sm font-medium text-gray-700 mb-1">Store promo image (upload)</label>
            <input type="file" name="store_image" accept="image/*" class="w-full px-3 py-2 border rounded-lg bg-white" />
            <% if (merchant.imageUrl) { %>
              <div class="mt-2">
                <div class="text-xs text-gray-500 mb-1">Current image</div>
                <img src="<%= merchant.imageUrl %>" alt="Store promo image" class="h-28 w-auto rounded-lg border bg-white object-cover" />
              </div>
            <% } %>
          </div>
          <div class="md:col-span-2">
            <label class="block text-sm font-medium text-gray-700 mb-1">or Store promo image URL</label>
            <input name="imageUrl" value="<%= merchant.imageUrl || '' %>" class="w-full px-3 py-2 border rounded-lg" />
            <div class="mt-1 text-xs text-gray-500">If both are provided, the uploaded image will be used.</div>
          </div>
          <div class="md:col-span-2">
            <button class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700" type="submit">Save profile</button>
          </div>
        </form>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <!-- Packages -->
        <div class="bg-white border rounded-xl shadow-sm p-5">
          <div class="text-sm font-semibold text-gray-900">Publish group-buy packages</div>
          <form class="mt-4 space-y-3" method="post" action="/merchant/packages" enctype="multipart/form-data">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Name *</label>
                <input name="name" required class="w-full px-3 py-2 border rounded-lg" />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Group-buy price *</label>
                <input name="price" required class="w-full px-3 py-2 border rounded-lg" />
              </div>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Description</label>
              <textarea name="description" rows="3" class="w-full px-3 py-2 border rounded-lg"></textarea>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Region</label>
                <input name="region" placeholder="Central/West/North Shore/East" class="w-full px-3 py-2 border rounded-lg" />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Image (upload)</label>
                <input type="file" name="image" accept="image/*" class="w-full px-3 py-2 border rounded-lg bg-white" />
              </div>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">or Image URL</label>
              <input name="imageUrl" class="w-full px-3 py-2 border rounded-lg" />
              <div class="mt-1 text-xs text-gray-500">If both are provided, the uploaded image will be used.</div>
            </div>
            <div class="border rounded-lg p-3 bg-gray-50">
              <div class="text-sm font-medium text-gray-900">Advanced fields (optional)</div>
              <div class="mt-2 space-y-2">
                <div>
                  <label class="block text-xs font-medium text-gray-600 mb-1">itemsJson (default: [])</label>
                  <textarea name="itemsJson" rows="3" class="w-full px-3 py-2 border rounded-lg font-mono text-xs">[]</textarea>
                </div>
                <div class="flex items-center gap-2 text-sm text-gray-700">
                  <input type="checkbox" name="isActive" checked />
                  Publish immediately
                </div>
              </div>
            </div>
            <button class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700" type="submit">Create package</button>
          </form>

          <div class="mt-5 space-y-2">
            <% if (!packages || packages.length === 0) { %>
              <div class="text-sm text-gray-600">No packages yet</div>
            <% } else { %>
              <% packages.forEach(function(p){ %>
                <div class="border rounded-lg p-3 flex items-start justify-between gap-3">
                  <div class="min-w-0">
                    <div class="text-sm font-medium text-gray-900 truncate"><%= p.name %></div>
                    <div class="text-xs text-gray-500 mt-1">$<%= p.price %> · <%= p.isActive ? 'Active' : 'Inactive' %></div>
                  </div>
                  <div class="flex items-center gap-2">
                    <a href="/merchant/packages/<%= p.id %>" class="px-3 py-1.5 rounded-lg border text-gray-700 hover:bg-gray-50 text-sm">View</a>
                  <form method="post" action="/merchant/packages/<%= p.id %>/delete">
                      <button class="px-3 py-1.5 rounded-lg border text-gray-700 hover:bg-gray-50 text-sm" type="submit">Delete</button>
                  </form>
                  </div>
                </div>
              <% }); %>
            <% } %>
          </div>
        </div>

        <!-- Services -->
        <div class="bg-white border rounded-xl shadow-sm p-5">
          <div class="text-sm font-semibold text-gray-900">Publish services</div>
          <form class="mt-4 space-y-3" method="post" action="/merchant/services" enctype="multipart/form-data">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Name *</label>
                <input name="name" required class="w-full px-3 py-2 border rounded-lg" />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Price (display)</label>
                <input name="price" class="w-full px-3 py-2 border rounded-lg" />
              </div>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Description</label>
              <textarea name="description" rows="3" class="w-full px-3 py-2 border rounded-lg"></textarea>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Duration (mins)</label>
                <input name="durationMins" type="number" class="w-full px-3 py-2 border rounded-lg" />
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Image (upload)</label>
                <input type="file" name="image" accept="image/*" class="w-full px-3 py-2 border rounded-lg bg-white" />
              </div>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">or Image URL</label>
              <input name="imageUrl" class="w-full px-3 py-2 border rounded-lg" />
              <div class="mt-1 text-xs text-gray-500">If both are provided, the uploaded image will be used.</div>
            </div>
            <div class="border rounded-lg p-3 bg-gray-50">
              <div class="text-sm font-medium text-gray-900">Time slots (JSON array)</div>
              <textarea name="timeSlotsJson" rows="3" placeholder='["2026-02-01 10:00","2026-02-01 14:00"]' class="w-full px-3 py-2 border rounded-lg font-mono text-xs"></textarea>
              <div class="mt-2 flex items-center gap-2 text-sm text-gray-700">
                <input type="checkbox" name="isActive" checked />
                Publish immediately
              </div>
            </div>
            <button class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700" type="submit">Create service</button>
          </form>

          <div class="mt-5 space-y-2">
            <% if (!services || services.length === 0) { %>
              <div class="text-sm text-gray-600">No services yet</div>
            <% } else { %>
              <% services.forEach(function(s){ %>
                <div class="border rounded-lg p-3 flex items-start justify-between gap-3">
                  <div class="min-w-0">
                    <div class="text-sm font-medium text-gray-900 truncate"><%= s.name %></div>
                    <div class="text-xs text-gray-500 mt-1"><%= s.isActive ? 'Active' : 'Inactive' %></div>
                  </div>
                  <div class="flex items-center gap-2">
                    <a href="/merchant/services/<%= s.id %>" class="px-3 py-1.5 rounded-lg border text-gray-700 hover:bg-gray-50 text-sm">View</a>
                  <form method="post" action="/merchant/services/<%= s.id %>/delete">
                      <button class="px-3 py-1.5 rounded-lg border text-gray-700 hover:bg-gray-50 text-sm" type="submit">Delete</button>
                  </form>
                  </div>
                </div>
              <% }); %>
            <% } %>
          </div>
        </div>
      </div>

      <div class="bg-white border rounded-xl shadow-sm p-5">
        <div class="text-sm font-semibold text-gray-900">Quotes (custom service requests)</div>
        <div class="text-sm text-gray-600 mt-1">Go to quote hub: <a class="text-blue-600 hover:text-blue-800" href="/service-booking/merchant">/service-booking/merchant</a></div>
      </div>
    </div>
  </div>
</body>
</html>

