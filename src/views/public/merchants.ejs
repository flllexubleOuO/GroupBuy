<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Merchants - WeChat GroupBuy</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <%- include('../partials/topnav', {
      title: 'Merchants',
      subtitle: 'Search stores (defaults to popular merchants)',
      containerClass: 'max-w-5xl'
    }) %>

    <div class="max-w-5xl mx-auto px-4 py-6 space-y-6">
      <section class="bg-white rounded-xl shadow-sm border p-4">
        <form method="get" action="/merchants" class="flex flex-col sm:flex-row gap-3 items-stretch sm:items-end">
          <div class="flex-1">
            <label class="block text-sm font-medium text-gray-700 mb-1">Search</label>
            <input
              name="q"
              value="<%= q || '' %>"
              placeholder="Store name / keywords / address..."
              class="w-full px-3 py-2 border rounded-lg"
            />
          </div>
          <button class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700" type="submit">Search</button>
          <% if (q) { %>
            <a href="/merchants" class="px-4 py-2 rounded-lg bg-gray-100 text-gray-700 hover:bg-gray-200 text-center">Clear</a>
          <% } %>
        </form>
        <div class="mt-2 text-xs text-gray-500">
          <% if (q) { %>
            Showing results for "<%= q %>"
          <% } else { %>
            Showing popular merchants (by # of listings)
          <% } %>
        </div>
      </section>

      <section class="space-y-3">
        <% if (merchants && merchants.length > 0) { %>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
            <% merchants.forEach(function(m) { %>
              <a href="/merchants/<%= m.id %>" class="bg-white rounded-xl shadow-sm border p-4 flex gap-3 hover:border-blue-300 transition">
                <div class="w-20 h-20 flex-shrink-0 rounded-lg bg-gray-100 border overflow-hidden">
                  <% if (m.imageUrl) { %>
                    <img src="<%= m.imageUrl %>" alt="<%= m.name %>" class="w-full h-full object-cover" />
                  <% } else { %>
                    <div class="w-full h-full flex items-center justify-center text-[10px] text-gray-400">No image</div>
                  <% } %>
                </div>
                <div class="flex-1 min-w-0">
                  <div class="flex items-start justify-between gap-2">
                    <div class="min-w-0">
                      <div class="text-sm font-semibold text-gray-900 truncate"><%= m.name %></div>
                      <div class="text-xs text-gray-500 line-clamp-2"><%= m.description || '' %></div>
                    </div>
                    <div class="text-xs text-gray-500 flex-shrink-0 text-right">
                      <div><%= (m._count && typeof m._count.packages !== 'undefined') ? m._count.packages : 0 %> group buys</div>
                      <div><%= (m._count && typeof m._count.services !== 'undefined') ? m._count.services : 0 %> services</div>
                    </div>
                  </div>

                  <div class="mt-2 grid grid-cols-1 gap-1 text-xs text-gray-600">
                    <% if (m.openHours) { %>
                      <div><span class="text-gray-500">Hours:</span> <%= m.openHours %></div>
                    <% } %>
                    <% if (m.address) { %>
                      <div><span class="text-gray-500">Address:</span> <%= m.address %></div>
                    <% } %>
                  </div>
                </div>
              </a>
            <% }); %>
          </div>

          <% if (pagination && pagination.totalPages && pagination.totalPages > 1) { %>
            <div class="flex items-center justify-center gap-2 pt-2">
              <a
                href="/merchants?<%= queryString %>&page=<%= Math.max(1, pagination.page - 1) %>"
                class="px-3 py-2 rounded-lg border text-sm <%= pagination.page <= 1 ? 'text-gray-400 pointer-events-none bg-gray-50' : 'text-gray-700 hover:bg-gray-50' %>"
              >Prev</a>
              <div class="text-sm text-gray-600 px-2">
                Page <%= pagination.page %> / <%= pagination.totalPages %>
              </div>
              <a
                href="/merchants?<%= queryString %>&page=<%= Math.min(pagination.totalPages, pagination.page + 1) %>"
                class="px-3 py-2 rounded-lg border text-sm <%= pagination.page >= pagination.totalPages ? 'text-gray-400 pointer-events-none bg-gray-50' : 'text-gray-700 hover:bg-gray-50' %>"
              >Next</a>
            </div>
          <% } %>
        <% } else { %>
          <div class="bg-white rounded-xl shadow-sm border p-4 text-sm text-gray-600">
            No merchants found.
          </div>
        <% } %>
      </section>
    </div>
  </div>
</body>
</html>

