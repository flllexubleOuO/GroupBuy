<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Home - WeChat GroupBuy</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <%- include('../partials/topnav', {
      title: 'Group Buy & Service Booking',
      subtitle: 'Unified home entry (Phase 2 extension)',
      containerClass: 'max-w-5xl',
      navUsername
    }) %>

    <div class="max-w-5xl mx-auto px-4 py-6 space-y-8">
      <!-- Entry cards -->
      <section class="space-y-3">
        <div class="flex items-center justify-between">
          <h2 class="text-lg font-semibold text-gray-900">Quick Access</h2>
          <div class="text-xs text-gray-500">4 entries: Group Buy / Service Booking / Order Lookup / Merchant Dashboard</div>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3">
          <!-- Group Buying -->
          <a href="/order" class="bg-white rounded-xl shadow-sm border hover:border-blue-400 transition p-4 block">
            <div class="text-sm font-semibold text-gray-900">Group Buying</div>
            <div class="text-xs text-gray-500 mt-1">Go to the existing group-buy order page</div>
            <div class="mt-3 text-sm text-blue-600">Enter →</div>
          </a>

          <!-- Service Booking -->
          <a href="/service-booking" class="bg-white rounded-xl shadow-sm border hover:border-blue-400 transition p-4 block">
            <div class="text-sm font-semibold text-gray-900">Service Booking</div>
            <div class="text-xs text-gray-500 mt-1">New module entry (Phase 2)</div>
            <div class="mt-3 text-sm text-blue-600">Book now →</div>
          </a>

          <!-- Order Lookup -->
          <a href="/query-order" class="bg-white rounded-xl shadow-sm border hover:border-blue-400 transition p-4 block">
            <div class="text-sm font-semibold text-gray-900">Order Lookup</div>
            <div class="text-xs text-gray-500 mt-1">Search orders by phone number</div>
            <div class="mt-3 text-sm text-blue-600">Lookup →</div>
          </a>

          <!-- Merchant Dashboard -->
          <a href="/admin/login" class="bg-white rounded-xl shadow-sm border hover:border-blue-400 transition p-4 block">
            <div class="text-sm font-semibold text-gray-900">Merchant Dashboard</div>
            <div class="text-xs text-gray-500 mt-1">Admin login / management entry</div>
            <div class="mt-3 text-sm text-blue-600">Open dashboard →</div>
          </a>
        </div>
      </section>

      <!-- Highlights -->
      <section class="space-y-3">
        <div class="flex items-center justify-between">
          <h2 class="text-lg font-semibold text-gray-900">Highlights</h2>
          <div class="text-xs text-gray-500">Top 5 (by sortOrder / updated time)</div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
          <!-- Hot group buy packages -->
          <div class="bg-white rounded-xl shadow-sm border p-4">
            <div class="flex items-center justify-between mb-3">
              <div class="text-sm font-semibold text-gray-900">Hot Group-Buy Packages</div>
              <a href="/order" class="text-xs text-blue-600 hover:text-blue-800">View all →</a>
            </div>

            <% if (hotPackages && hotPackages.length > 0) { %>
              <% const regionLabel = { '中区': 'Central', '西区': 'West', '北岸': 'North Shore', '东区': 'East' }; %>
              <div class="space-y-3">
                <% hotPackages.forEach(function(pkg) { %>
                  <div class="flex gap-3 border rounded-lg p-3 hover:border-blue-300 transition">
                    <div class="w-16 h-16 flex-shrink-0 rounded bg-gray-100 border overflow-hidden">
                      <% if (pkg.imageUrl) { %>
                        <img src="<%= pkg.imageUrl %>" alt="<%= pkg.name %>" class="w-full h-full object-cover" />
                      <% } else { %>
                        <div class="w-full h-full flex items-center justify-center text-[10px] text-gray-400">No image</div>
                      <% } %>
                    </div>
                    <div class="flex-1 min-w-0">
                      <div class="flex items-start justify-between gap-2">
                        <div class="min-w-0">
                          <div class="text-sm font-medium text-gray-900 truncate"><%= pkg.name %></div>
                          <div class="text-xs text-gray-500 line-clamp-2"><%= pkg.description || '' %></div>
                          <% if (pkg.region) { %>
                            <div class="mt-1 text-[11px] text-gray-500">Region: <%= regionLabel[pkg.region] || pkg.region %></div>
                          <% } %>
                        </div>
                        <div class="text-right flex-shrink-0">
                          <% if (pkg.originalPrice) { %>
                            <div class="text-[11px] text-gray-400 line-through">$<%= Number(pkg.originalPrice).toFixed(2) %></div>
                            <div class="text-sm font-bold text-red-600">$<%= Number(pkg.price).toFixed(2) %></div>
                          <% } else { %>
                            <div class="text-sm font-bold text-blue-600">$<%= Number(pkg.price).toFixed(2) %></div>
                          <% } %>
                        </div>
                      </div>

                      <div class="mt-2 flex items-center gap-2">
                        <a href="/order?packageId=<%= pkg.id %>" class="text-xs text-blue-600 hover:text-blue-800">Order this →</a>
                        <span class="text-gray-300">|</span>
                        <a href="/order" class="text-xs text-gray-600 hover:text-gray-800">More packages</a>
                      </div>
                    </div>
                  </div>
                <% }); %>
              </div>
            <% } else { %>
              <div class="text-sm text-gray-500">
                No package data yet (create packages first, or use mock data).
              </div>
            <% } %>
          </div>

          <!-- Hot services (placeholder) -->
          <div class="bg-white rounded-xl shadow-sm border p-4">
            <div class="flex items-center justify-between mb-3">
              <div class="text-sm font-semibold text-gray-900">Hot Services (Reserved)</div>
              <a href="/service-booking" class="text-xs text-blue-600 hover:text-blue-800">Go to Service Booking →</a>
            </div>

            <div class="space-y-3">
              <div class="border rounded-lg p-3 bg-gray-50">
                <div class="text-sm font-medium text-gray-900">(Mock) Home Cleaning</div>
                <div class="text-xs text-gray-500 mt-1">Will support time slots, reference image upload, and status flow (TBD)</div>
              </div>
              <div class="border rounded-lg p-3 bg-gray-50">
                <div class="text-sm font-medium text-gray-900">(Mock) Appliance Installation</div>
                <div class="text-xs text-gray-500 mt-1">Loosely coupled with orders; can later extend to coupons / bundle promos</div>
              </div>
              <div class="text-xs text-gray-500">
                Note: this phase only reserves the entry & structure; Service/Booking models and flows will be added next.
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Footer -->
      <footer class="text-center text-xs text-gray-400 py-6">
        © <%= new Date().getFullYear() %> GroupBuy · Phase 2 Extension
      </footer>
    </div>
  </div>
</body>
</html>

