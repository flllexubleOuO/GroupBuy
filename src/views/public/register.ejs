<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <%- include('../partials/topnav', { title: 'Register', containerClass: 'max-w-lg' }) %>

    <div class="max-w-lg mx-auto px-4 py-10">
      <div class="bg-white border rounded-xl shadow-sm p-6">
        <div class="text-lg font-semibold text-gray-900">Create an account</div>
        <div class="text-sm text-gray-500 mt-1">Email + phone + password</div>

        <% if (error) { %>
          <div class="mt-4 bg-red-50 border border-red-200 rounded-lg p-3 text-sm text-red-800"><%= error %></div>
        <% } %>

        <form class="mt-5 space-y-4" method="post" action="/register">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Email *</label>
            <input type="email" name="email" required value="<%= form?.email || '' %>" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Phone *</label>
            <input name="phone" required value="<%= form?.phone || '' %>" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Password *</label>
            <input type="password" name="password" required class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
            <div class="mt-1 text-xs text-gray-500">At least 6 characters</div>
          </div>

          <button class="w-full py-2.5 rounded-lg bg-blue-600 text-white font-semibold hover:bg-blue-700" type="submit">Register & Login</button>
        </form>

        <div class="mt-4 text-sm">
          <a class="text-blue-600 hover:text-blue-800" href="/login">Already have an account? Login</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>

