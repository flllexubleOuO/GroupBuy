<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Payment - GroupBuy</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <%- include('../partials/topnav', {
      title: 'Payment',
      subtitle: (typeof userEmail !== 'undefined' && userEmail) ? userEmail : '',
      containerClass: 'max-w-5xl'
    }) %>

    <div class="max-w-5xl mx-auto px-4 py-6 space-y-6">
      <% if (error) { %>
        <div class="bg-red-50 border border-red-200 rounded-xl p-4 text-sm text-red-800"><%= error %></div>
      <% } %>

      <div class="bg-white border rounded-xl shadow-sm p-5">
        <div class="text-sm font-semibold text-gray-900">Order</div>
        <div class="mt-2 text-sm text-gray-600">
          ID: <span class="font-mono"><%= String(order.id).slice(0, 8) %></span> · Placed at: <%= new Date(order.createdAt).toLocaleString() %>
        </div>
        <div class="mt-3 grid grid-cols-1 md:grid-cols-2 gap-3 text-sm">
          <div class="bg-gray-50 border rounded-lg p-3">
            <div class="text-xs text-gray-500">Delivery</div>
            <div class="mt-1"><strong>Name:</strong> <%= order.customerName %></div>
            <div class="mt-1"><strong>Phone:</strong> <%= order.phone %></div>
            <div class="mt-1"><strong>Address:</strong> <%= order.address %></div>
            <div class="mt-1"><strong>Time:</strong> <%= order.deliveryTime %></div>
          </div>
          <div class="bg-gray-50 border rounded-lg p-3">
            <div class="text-xs text-gray-500">Items</div>
            <div class="mt-2 space-y-2">
              <% (items || []).forEach(function(it){ %>
                <div class="flex justify-between gap-3">
                  <div class="min-w-0">
                    <div class="text-sm text-gray-800 truncate"><%= it.title %></div>
                    <div class="text-xs text-gray-500">Qty: <%= it.quantity %></div>
                  </div>
                  <div class="text-sm font-semibold text-gray-900">$<%= Number(it.price).toFixed(2) %></div>
                </div>
              <% }); %>
            </div>
          </div>
        </div>
      </div>

      <div class="bg-white border rounded-xl shadow-sm p-5">
        <div class="text-sm font-semibold text-gray-900">Pay</div>
        <form class="mt-4 space-y-4" method="post" action="/payment/<%= order.id %>" enctype="multipart/form-data">
          <div class="space-y-2">
            <div class="text-sm font-medium text-gray-700">Payment method *</div>
            <label class="flex items-start gap-3 p-3 border rounded-lg cursor-pointer">
              <input type="radio" name="paymentMethod" value="transfer" checked class="mt-1" />
              <div>
                <div class="text-sm font-medium text-gray-900">Bank transfer</div>
                <div class="text-xs text-gray-600 mt-1">Transfer then upload your payment proof.</div>
                <div class="text-xs text-gray-500 mt-1 break-all">Bank account: XX-XXXX-XXXXXX-XX</div>
              </div>
            </label>
            <label class="flex items-start gap-3 p-3 border rounded-lg cursor-pointer">
              <input type="radio" name="paymentMethod" value="cash_on_delivery" class="mt-1" />
              <div>
                <div class="text-sm font-medium text-gray-900">Cash on delivery</div>
                <div class="text-xs text-gray-600 mt-1">Pay on arrival. No upload required.</div>
              </div>
            </label>
          </div>

          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Payment proof (for bank transfer)</label>
            <input type="file" name="payment_screenshot" accept="image/*" class="w-full px-3 py-2 border rounded-lg bg-white" />
            <div class="mt-1 text-xs text-gray-500">JPG/PNG/GIF/WebP up to 5MB.</div>
          </div>

          <button class="w-full px-4 py-3 rounded-lg bg-blue-600 text-white hover:bg-blue-700" type="submit">
            Submit payment
          </button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>

