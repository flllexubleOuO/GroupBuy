<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Request Detail - Merchant</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <div class="bg-white shadow-sm sticky top-0 z-10">
      <div class="max-w-4xl mx-auto px-4 py-4 flex items-center justify-between">
        <div class="text-lg font-semibold text-gray-900">Request Detail</div>
        <div class="flex items-center gap-3">
          <a href="/service-booking/merchant<%= key ? ('?key=' + encodeURIComponent(key)) : '' %>" class="text-sm text-blue-600 hover:text-blue-800">Dashboard</a>
          <a href="/home" class="text-sm text-blue-600 hover:text-blue-800">Home</a>
        </div>
      </div>
    </div>

    <div class="max-w-4xl mx-auto px-4 py-6 space-y-4">
      <div class="bg-white border rounded-xl shadow-sm p-5">
        <div class="flex items-start justify-between gap-4">
          <div>
            <div class="text-sm text-gray-500">Type</div>
            <div class="text-lg font-semibold text-gray-900"><%= request.serviceType %></div>
            <% if (request.title) { %>
              <div class="text-sm text-gray-700 mt-1"><%= request.title %></div>
            <% } %>
            <div class="text-xs text-gray-500 mt-2">Status: <span class="font-medium"><%= request.status %></span></div>
          </div>
          <div class="text-right text-xs text-gray-500">
            <div>Request ID</div>
            <div class="font-mono text-gray-700"><%= request.id %></div>
          </div>
        </div>

        <div class="mt-4 grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <div class="text-sm font-medium text-gray-900">Description</div>
            <div class="mt-1 text-sm text-gray-700 whitespace-pre-line"><%= request.description %></div>
            <% if (request.address) { %>
              <div class="mt-3 text-sm"><span class="text-gray-500">Address:</span> <%= request.address %></div>
            <% } %>
            <% if (request.preferredTime) { %>
              <div class="mt-1 text-sm"><span class="text-gray-500">Preferred time:</span> <%= request.preferredTime %></div>
            <% } %>
          </div>
          <div>
            <div class="text-sm font-medium text-gray-900">Reference image</div>
            <div class="mt-2">
              <% if (displayImage) { %>
                <img src="<%= displayImage %>" class="max-w-full h-40 object-contain border rounded bg-gray-50" />
              <% } else { %>
                <div class="text-sm text-gray-500">No image uploaded.</div>
              <% } %>
            </div>
          </div>
        </div>
      </div>

      <div class="bg-white border rounded-xl shadow-sm p-5">
        <div class="text-sm font-semibold text-gray-900">Your quote (only visible to the user)</div>
        <div class="text-xs text-gray-500 mt-1">Other merchants cannot see your price/details.</div>

        <% const defaultContact = `Phone: ${merchant.phone || ''}\nWeChat: ${merchant.wechat || ''}\nEmail: ${merchant.email || ''}`.trim(); %>
        <form action="/service-booking/merchant/requests/<%= request.id %>/quote<%= key ? ('?key=' + encodeURIComponent(key)) : '' %>" method="post" class="mt-4 space-y-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Price *</label>
            <input name="price" type="number" step="0.01" min="0" required value="<%= myQuote ? myQuote.price : '' %>" placeholder="e.g. 120.00" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
          </div>

          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Details (optional)</label>
            <textarea name="details" rows="4" placeholder="Scope, what’s included, exclusions, timing, etc." class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"><%= myQuote && myQuote.details ? myQuote.details : '' %></textarea>
          </div>

          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Contact info (optional)</label>
            <textarea name="contactInfo" rows="3" placeholder="Phone / WeChat / Email (user will contact you outside the platform)" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"><%= myQuote && myQuote.contactInfo ? myQuote.contactInfo : (defaultContact ? defaultContact : '') %></textarea>
          </div>

          <div class="flex items-center gap-3">
            <button type="submit" class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700">Submit quote</button>
            <a href="/service-booking/merchant<%= key ? ('?key=' + encodeURIComponent(key)) : '' %>" class="px-4 py-2 rounded-lg border text-gray-700 hover:bg-gray-50">Back</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>

