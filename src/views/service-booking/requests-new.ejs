<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Post a Custom Service Request</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <div class="bg-white shadow-sm sticky top-0 z-10">
      <div class="max-w-3xl mx-auto px-4 py-4 flex items-center justify-between">
        <div>
          <div class="text-xl font-bold text-gray-900">Custom Service Request</div>
          <div class="text-sm text-gray-500">Describe your needs, merchants will send private quotes</div>
        </div>
        <div class="flex items-center gap-3">
          <a href="/service-booking" class="text-sm text-blue-600 hover:text-blue-800">Back</a>
          <a href="/home" class="text-sm text-blue-600 hover:text-blue-800">Home</a>
        </div>
      </div>
    </div>

    <div class="max-w-3xl mx-auto px-4 py-6">
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 text-red-800 text-sm mb-4"><%= error %></div>
      <% } %>

      <form action="/service-booking/requests" method="post" enctype="multipart/form-data" class="bg-white border rounded-xl shadow-sm p-5 space-y-4">
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Your name *</label>
            <input name="userName" required class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Phone *</label>
            <input name="userPhone" required class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Service type *</label>
          <select name="serviceType" required class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
            <% serviceTypes.forEach(function(t) { %>
              <option value="<%= t %>"><%= t %></option>
            <% }); %>
          </select>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Title (optional)</label>
          <input name="title" placeholder="e.g. 2-bedroom apartment cleaning" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Describe your needs *</label>
          <textarea name="description" required rows="6" placeholder="What needs to be done? Size/rooms, special requests, pets, parking, materials needed, etc." class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"></textarea>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Address (optional)</label>
            <input name="address" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Preferred time (optional)</label>
            <input name="preferredTime" placeholder="e.g. Weekend morning" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Reference image (optional)</label>
          <input type="file" name="reference_image" accept="image/*" class="w-full px-3 py-2 border rounded-lg" />
          <div class="mt-1 text-xs text-gray-500">JPG/PNG/GIF/WebP up to 5MB.</div>
        </div>

        <div class="text-xs text-gray-500">
          Notes: merchants can see your request details, but <span class="font-medium">each quote is only visible to you</span>. No in-app chat; merchants may leave contact info in their quote.
        </div>

        <div class="flex items-center gap-3">
          <button class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700" type="submit">Publish request</button>
          <a href="/service-booking" class="px-4 py-2 rounded-lg border text-gray-700 hover:bg-gray-50">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>

