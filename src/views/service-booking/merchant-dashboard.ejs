<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Merchant Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <%- include('../partials/topnav', { title: 'Merchant Quotes', subtitle: merchant.name + ' · Open requests', containerClass: 'max-w-5xl' }) %>

    <div class="max-w-5xl mx-auto px-4 py-6 space-y-4">
      <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 text-sm text-yellow-900">
        <% if (key) { %>
          Phase 2 note: legacy access via <span class="font-mono">?key=...</span>. Logged-in merchants no longer need this key.
        <% } else { %>
          You are logged in as merchant. (Legacy <span class="font-mono">?key=...</span> is still supported for old links.)
        <% } %>
      </div>

      <div class="bg-white border rounded-xl shadow-sm p-4 flex items-center justify-between gap-3">
        <div class="text-sm text-gray-700">
          Quick links:
          <a class="text-blue-600 hover:text-blue-800" href="/merchant/dashboard">Store dashboard</a>
          <span class="text-gray-300 mx-1">|</span>
          <a class="text-blue-600 hover:text-blue-800" href="/service-booking">Service booking</a>
        </div>
      </div>

      <% if (!openRequests || openRequests.length === 0) { %>
        <div class="bg-white border rounded-xl shadow-sm p-5 text-sm text-gray-600">No open requests right now.</div>
      <% } else { %>
        <div class="space-y-3">
          <% openRequests.forEach(function(r) { %>
            <a href="/service-booking/merchant/requests/<%= r.id %><%= key ? ('?key=' + encodeURIComponent(key)) : '' %>" class="bg-white border rounded-xl shadow-sm p-4 block hover:border-blue-300 transition">
              <div class="flex items-start justify-between gap-3">
                <div class="min-w-0">
                  <div class="text-sm font-semibold text-gray-900"><%= r.serviceType %></div>
                  <div class="text-xs text-gray-500 mt-1"><%= r.title || '' %></div>
                  <div class="text-xs text-gray-500 mt-1">Posted: <%= new Date(r.createdAt).toLocaleString() %></div>
                </div>
                <div class="text-right flex-shrink-0">
                  <% if (quotedSet && quotedSet.has(r.id)) { %>
                    <div class="text-xs font-medium text-green-700 bg-green-50 border border-green-200 px-2 py-1 rounded-full">Quoted</div>
                  <% } else { %>
                    <div class="text-xs font-medium text-gray-700 bg-gray-50 border px-2 py-1 rounded-full">Not quoted</div>
                  <% } %>
                </div>
              </div>
              <div class="mt-2 text-sm text-gray-700 line-clamp-2 whitespace-pre-line"><%= r.description %></div>
            </a>
          <% }); %>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>

